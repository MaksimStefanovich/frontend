// Известно, что X кг конфет стоит A рублей. Определить, сколько стоит 1 кг и Y кг этих же конфет.

{
function oneKg(a , x){
const oneKg = a / x;
return costOneKg;
}

function task1a(a, x, y)
{
  oneKg = oneKg(a , x);
  let yKg = oneKg * y:
}

function task1b(a, b, x, y)
{
  oneKgA = oneKg(a , x);
  oneKgB = oneKg(b , y);
 return let dif = oneKgA / oneKgB;
}

function triangle( a, b, c)
{
 if(a == b || a == c || b == c ) return true;
 return false;
}

function distance(a, b, c)
{
if ((c — a) < (b — a))
 abs(c — a);
else
 abs(b — a);
}
function sFact(n)
{
    var rval =1;
    for (var i = 2; i <= n; i++)
        rval = rval * i;
    return rval;
}
}